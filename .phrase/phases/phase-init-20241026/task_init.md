# Tasks: Recon Improvements and CI Setup

- task001 [x] Setup `.github/workflows/ci.yml` + verification method: CI runs on push.
- task002 [x] Add API/Full pipeline templates to `hyper-recon-pipeline` + verification: UI shows new templates.
- task003 [x] Add HTML export to `hyper-findings-log` + verification: Export button works and generates HTML.
